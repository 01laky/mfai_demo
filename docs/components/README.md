# Components catalog

Each file here describes an **implemented** reusable slice of the system: purpose, main paths in the repo, and a link to the full guide in [`../guides/`](../guides/).

Add a new markdown file when you ship a coherent module (e.g. “Wall grid tile”, “OAuth client wrapper”) so agents and humans find it quickly.
